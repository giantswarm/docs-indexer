---
# Helm values for the app-test-suite install of execute-chart-tests
# (.ats/main.yaml). The chart's defaults install as they are: the CronJobs are
# created but never run on the kind cluster, so the dummy credentials and the
# unreachable search endpoint do not matter.
